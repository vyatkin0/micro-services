{
  "name": "micro-products",
  "version": "0.0.1",
  "scripts": {
    "build": "rimraf ./build && tsc && copy .\\protos\\products.proto .\\build",
    "start": "npm run build && node build/products.js"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.1.0",
    "@grpc/proto-loader": "^0.5.0",
    "google-protobuf": "^3.0.0",
    "jsonwebtoken": "^8.5.1",
    "sqlite3": "^5.1.2"
  },
  "devDependencies": {
    "@types/node": "^18.11.4",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  }
}
